# Этот файл больше не является основной точкой входа.
# Основное приложение находится в app/main.py.
# Запуск осуществляется через `uvicorn app.main:app`.

# Оставляем этот файл для обратной совместимости,
# но он должен просто импортировать и запустить основное приложение.

from app.main import app
import uvicorn

if __name__ == "__main__":
    print("Запуск через устаревший main.py. Используйте `uvicorn app.main:app` для запуска.")
    uvicorn.run(app, host="0.0.0.0", port=8000)
